# params.yml — bigbio:quantmsdiann
# Only parameters that differ from the pipeline defaults (validated against
# ../assets/nextflow_schema.json).
#
# `database` is REQUIRED and has no default: it must always be set to a protein
# FASTA (.fasta/.fa) on the cluster. For DIA data the FASTA must NOT contain
# decoys (they are generated internally). Include contaminants as needed.
# EDIT this to your protein database, or override with --set database=...
database: /data/$USER/databases/uniprot/UP000005640_9606.fasta
